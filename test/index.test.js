var hadronApplicationMenu = require('../');
var assert = require('assert');

describe('hadron-application-menu', function() {
  it('should work', function() {
    assert(hadronApplicationMenu);
  });
});
